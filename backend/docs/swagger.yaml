openapi: 3.0.0
info:
  title: Dayflow HRMS Backend API
  description: Complete REST API documentation for the Dayflow HRMS platform.
  version: 1.0.0
servers:
  - url: http://localhost:5000/api
    description: Local Development Server

paths:
  /auth/signup:
    post:
      summary: Register a new user
      tags: [Auth]
      responses:
        200:
          description: OK